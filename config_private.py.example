"""
Private configuration settings - EXAMPLE/TEMPLATE
Copy this file to config_private.py and update with your actual credentials
"""

from typing import Dict, Any

# Your actual camera credentials
RTSP_CONFIG_PRIVATE: Dict[str, Any] = {
    'username': 'admin',
    'password': 'your_camera_password',
    'ip_address': '192.168.1.XXX',
    'port': 554,
}

# Any other private settings you want to override
APP_CONFIG_PRIVATE: Dict[str, Any] = {
    # 'secret_key': 'your-production-secret-key-here',
    # 'debug': False,
    # 'port': 5005,
}

# Recording settings override (optional)
# RECORDING_CONFIG_PRIVATE: Dict[str, Any] = {
#     'crf': 28,  # Compression level: 18=high quality, 28=medium, 35=low quality
#     'preset': 'faster',  # Encoding speed: ultrafast, faster, fast, medium, slow
#     'resolution': None,  # Downscale: '1280x720', '640x480', or None for original
#     'max_file_size_mb': 10,  # Max file size before rotation (default: 10 MB)
# }
